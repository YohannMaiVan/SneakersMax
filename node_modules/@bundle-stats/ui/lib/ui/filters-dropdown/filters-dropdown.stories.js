"use strict";

var _react = _interopRequireDefault(require("react"));

var _react2 = require("@storybook/react");

var _stories = require("../../stories");

var _ = require(".");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var stories = (0, _react2.storiesOf)('UI/FiltersDropdown', module);
stories.addDecorator((0, _stories.getWrapperDecorator)({
  paddingLeft: '200px'
}));
stories.add('default', function () {
  return /*#__PURE__*/_react["default"].createElement(_.FiltersDropdown, {
    onChange: function onChange(state) {
      console.log(state); // eslint-disable-line no-console
    },
    filters: {
      changed: {
        label: 'Changed',
        defaultValue: true
      },
      entrypoint: {
        label: 'Entrypoint',
        defaultValue: false
      },
      fileTypes: {
        label: 'File types',
        CSS: {
          label: 'CSS',
          defaultValue: true
        },
        JS: {
          label: 'JS',
          defaultValue: true
        },
        HTML: {
          label: 'HTML',
          defaultValue: true
        }
      }
    }
  });
});
stories.add('disable options', function () {
  return /*#__PURE__*/_react["default"].createElement(_.FiltersDropdown, {
    onChange: function onChange(state) {
      console.log(state); // eslint-disable-line no-console
    },
    filters: {
      changed: {
        label: 'Changed',
        defaultValue: false,
        disabled: true
      },
      entrypoint: {
        label: 'Entrypoint',
        defaultValue: false
      },
      fileTypes: {
        label: 'File types',
        CSS: {
          label: 'CSS',
          defaultValue: true
        },
        JS: {
          label: 'JS',
          defaultValue: true
        },
        HTML: {
          label: 'HTML',
          defaultValue: true
        }
      }
    }
  });
});